# Core control stack
python-can>=4.3
numpy>=1.24
onnxruntime>=1.16

# IMU (Raspberry Pi only -- these pull in the CircuitPython/Blinka stack and
# will not install on a development laptop. Skip them there and use
# --no-imu / --virtual, which fall back to sensors.base.NullImu.)
adafruit-circuitpython-bno08x>=1.2 ; platform_machine == "aarch64"
adafruit-blinka>=8.0 ; platform_machine == "aarch64"

# Analysis only (plot.py, ik-visualizer.py)
matplotlib>=3.7
